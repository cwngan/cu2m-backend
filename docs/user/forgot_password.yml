Create a forgot password token
---
tags:
  - User
parameters:
  - name: body
    in: body
    required: true
    schema:
      id: UserForgotPasswordModel
      required:
        - email
      properties:
        email:
          type: string
          description: Signup email.
          example: "email@example.com"
responses:
  200:
    description: Created a forgot password token.
    schema:
      $ref: "#/definitions/ResponseModel"
